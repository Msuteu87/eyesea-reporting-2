workflows:
  ios-release:
    name: iOS Release
    max_build_duration: 60
    instance_type: mac_mini_m2
    working_directory: eyesea_reporting_2  # Set working directory to Flutter app folder
    integrations:
      app_store_connect: Eyesea_reporting_codemagic  # Set this name in Codemagic UI → Integrations
    environment:
      # Option 1: Use groups (preferred if working)
      groups:
        - eyesea_secrets  # Create this group in Codemagic UI → Environment Variables
      # Option 2: Use individual vars (if groups give encryption errors - uncomment below and comment out groups)
      # vars:
      #   SUPABASE_URL: Encrypted(...)  # Copy from Codemagic UI after creating variable
      #   SUPABASE_ANON_KEY: Encrypted(...)
      #   MAPBOX_ACCESS_TOKEN: Encrypted(...)
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.mariussuteu.eyesea.eyeseareporting
      flutter: stable
      xcode: latest
      cocoapods: default
    scripts:
      - name: Get Flutter dependencies
        script: |
          cd eyesea_reporting_2
          flutter pub get
      - name: Install CocoaPods
        script: |
          cd eyesea_reporting_2/ios
          pod install
      - name: Build iOS
        script: |
          cd eyesea_reporting_2
          flutter build ipa --release \
            --dart-define=SUPABASE_URL=$SUPABASE_URL \
            --dart-define=SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY \
            --dart-define=MAPBOX_ACCESS_TOKEN=$MAPBOX_ACCESS_TOKEN
    artifacts:
      - eyesea_reporting_2/build/ios/ipa/*.ipa
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true

  android-release:
    name: Android Release
    max_build_duration: 60
    instance_type: mac_mini_m2
    working_directory: eyesea_reporting_2  # Set working directory to Flutter app folder
    environment:
      # Option 1: Use groups (preferred if working)
      groups:
        - eyesea_secrets  # Create this group in Codemagic UI → Environment Variables
      # Option 2: Use individual vars (if groups give encryption errors - uncomment below and comment out groups)
      # vars:
      #   SUPABASE_URL: Encrypted(...)  # Copy from Codemagic UI after creating variable
      #   SUPABASE_ANON_KEY: Encrypted(...)
      #   MAPBOX_ACCESS_TOKEN: Encrypted(...)
      android_signing:
        - eyesea_keystore  # Reference name from Codemagic UI → Code signing identities
      flutter: stable
      java: 17
    scripts:
      - name: Get Flutter dependencies
        script: |
          flutter pub get
      - name: Build Android App Bundle
        script: |
          flutter build appbundle --release \
            --dart-define=SUPABASE_URL=$SUPABASE_URL \
            --dart-define=SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY \
            --dart-define=MAPBOX_ACCESS_TOKEN=$MAPBOX_ACCESS_TOKEN
    artifacts:
      - build/app/outputs/bundle/release/*.aab
      - build/app/outputs/mapping/release/mapping.txt  # ProGuard mapping file for crash deobfuscation
    publishing:
      google_play:
        credentials: Encrypted(...)  # Service account JSON from Google Play Console
        track: internal

  all-platforms:
    name: Build All Platforms
    max_build_duration: 90
    instance_type: mac_mini_m2
    working_directory: eyesea_reporting_2  # Set working directory to Flutter app folder
    integrations:
      app_store_connect: Eyesea_reporting_codemagic  # Set this name in Codemagic UI → Integrations
    environment:
      # Option 1: Use groups (preferred if working)
      groups:
        - eyesea_secrets  # Create this group in Codemagic UI → Environment Variables
      # Option 2: Use individual vars (if groups give encryption errors - uncomment below and comment out groups)
      # vars:
      #   SUPABASE_URL: Encrypted(...)  # Copy from Codemagic UI after creating variable
      #   SUPABASE_ANON_KEY: Encrypted(...)
      #   MAPBOX_ACCESS_TOKEN: Encrypted(...)
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.mariussuteu.eyesea.eyeseareporting
      android_signing:
        - eyesea_keystore
      flutter: stable
      xcode: latest
      cocoapods: default
      java: 17
    scripts:
      - name: Get Flutter dependencies
        script: |
          flutter pub get
      - name: Install CocoaPods
        script: |
          cd ios
          pod install
      - name: Build iOS
        script: |
          flutter build ipa --release \
            --dart-define=SUPABASE_URL=$SUPABASE_URL \
            --dart-define=SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY \
            --dart-define=MAPBOX_ACCESS_TOKEN=$MAPBOX_ACCESS_TOKEN
      - name: Build Android
        script: |
          flutter build appbundle --release \
            --dart-define=SUPABASE_URL=$SUPABASE_URL \
            --dart-define=SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY \
            --dart-define=MAPBOX_ACCESS_TOKEN=$MAPBOX_ACCESS_TOKEN
    artifacts:
      - build/ios/ipa/*.ipa
      - build/app/outputs/bundle/release/*.aab
      - build/app/outputs/mapping/release/mapping.txt  # ProGuard mapping file for crash deobfuscation
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
      google_play:
        credentials: Encrypted(...)
        track: internal
