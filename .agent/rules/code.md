---
trigger: always_on
---

### Code Generation
* **Build Runner:** Use `build_runner` for JSON serialization and other generation tasks.
* **Command:** `dart run build_runner build --delete-conflicting-outputs`.